const Login = () => {
  const estilos = {
    contenedor:
      "text-center flex flex-col pt-32 items-center w-full h-screen gap-12  bg-slate-500",
    titulo: "font-bold text-4xl",
  };

  return (
    <section className={estilos.contenedor}>
      <h2 className={estilos.contenedor}></h2>
    </section>
  );
};

export default Login;
